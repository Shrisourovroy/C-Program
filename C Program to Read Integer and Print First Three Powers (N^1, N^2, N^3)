#include<stdio.h>
#include<math.h>
int main()
{
    int n, a, b, c;
    printf("Enter the number: ");
    scanf("%d",&n);
    
    a= pow(n,1);
    b= pow(n,2);
    c= pow(n,3);


    printf("%d %d %d", a,b,c);
return 0;
}
