
#include<stdio.h>
int main()
{
    int i,n,fibo, first=0, second=1, count=0;
    scanf("%d",&n);
    for(i=1; i<=n; i++)
    {
        if(count<=1)
        {
        fibo=count;
        }
        else
            fibo= first+second;
            first=second;
            second=fibo;
             count++;
    printf("%d ",fibo);
    }
return 0;
}
